#pragma once

// UNO Q uses firmware/uno_q_serial_bridge.py for HTTP, so WiFi credentials are
// not needed by the Arduino sketch.
const char* BACKEND_HOST = "192.168.1.10";
const int BACKEND_PORT = 3000;
const char* DEVICE_ID = "ARD-001";
